
#ifndef _KEYBOARD_H   // Definitions so that the
#define _KEYBOARD_H  // file is included only once


void initKeyboard();

int32_t readKeyboard(void);

void intConfigKeyboard();

int32_t readMultiKey(void);

#endif
